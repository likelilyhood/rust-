# logscope 实时日志分析平台项目报告

## 封面信息

- 课程名称：____________
- 项目名称：logscope 实时日志分析平台
- 学院专业：____________
- 班级：____________
- 组长：____________
- 组员：____________
- 学号：____________
- 指导教师：____________
- 提交日期：____________

## 摘要

本项目设计并实现了一个基于 Rust 的实时日志分析平台 `logscope`。系统能够从标准输入、日志文件和 TCP 端口接收日志数据，对日志进行结构化解析，并在服务运行过程中持续维护实时指标。系统通过 HTTP API 输出分析结果，同时提供静态网页仪表盘展示关键指标，使用户能够实时观察请求总量、错误率、状态码分布、热点路径以及延迟分位数等信息。

在满足课程大作业基本要求的基础上，本项目进一步实现了并发解析 pipeline、多时间窗口统计、延迟分位数分析、背压与采样策略、告警机制、第二解析器、日志生成器以及跨平台 CI 检查等增强功能。项目整体采用 Rust workspace 组织，模块划分清晰，测试与验证较为完整，具备较好的可扩展性和演示效果。

## 关键词

Rust；实时日志分析；并发 pipeline；时间窗口；延迟分位数；告警系统；可视化仪表盘

## 目录

1. 项目背景与研究意义
2. 需求分析
3. 系统总体设计
4. 详细设计与实现
5. 系统测试与结果分析
6. 项目亮点与创新点
7. 存在问题与改进方向
8. 总结

## 1. 项目背景与研究意义

### 1.1 项目背景

在现代软件系统中，日志是定位故障、分析性能和观察服务健康状态的重要依据。传统方式通常依赖人工查看文本日志，难以及时掌握系统的整体变化趋势，也不利于在短时间内发现错误率升高、延迟波动等关键问题。

因此，本项目尝试实现一个轻量级、可实时展示、适合课程答辩演示的日志分析平台。系统能够接收日志流，将原始文本解析成统一结构，并持续更新聚合指标，最后通过网页界面和 API 向用户展示系统运行状态。

### 1.2 研究意义

本项目的意义主要体现在以下几个方面：

- 将“日志文件查看”升级为“实时指标分析”，增强系统观察能力
- 体现了流式处理、并发解析、窗口统计等工程化思路
- 适合作为 Rust 语言、系统编程或软件工程课程的大作业实践
- 为后续扩展更复杂的日志平台提供基础框架

### 1.3 项目目标

本项目的目标如下：

- 实现一个跨平台 Rust 项目，可在 macOS 与 Windows 上运行
- 支持多输入源接收日志
- 将原始日志解析为统一结构化事件
- 维护实时聚合指标并通过 API 暴露
- 提供可视化 dashboard
- 在基础功能上加入若干具有工程价值的扩展能力

## 2. 需求分析

### 2.1 基本功能需求

根据课程大作业要求，系统至少需要具备以下能力：

- 读取标准输入或指定文件中的日志
- 将 JSON Lines 日志解析为结构化对象
- 对非法日志进行容错处理，不因坏行导致崩溃
- 统计总量、错误数、错误率、平均延迟、最大延迟、状态码分布和热点路径
- 提供 `GET /health` 与 `GET /metrics` 接口
- 托管一个简单网页仪表盘
- 编写单元测试并通过构建检查

### 2.2 扩展功能需求

为了提升项目完成度和答辩展示效果，本项目进一步实现了以下扩展：

- 并发解析 worker pool
- 多时间窗口统计
- 延迟分位数分析
- 背压与丢弃策略
- 错误率与 p95 告警
- TCP 输入源
- 第二解析器
- 日志生成器
- CI 自动化检查

### 2.3 非功能需求

项目除了功能需求外，还需满足以下非功能要求：

- 代码结构清晰，便于多人协作
- 依赖尽量精简，避免平台相关问题
- 具有可测试性与可维护性
- 页面展示稳定、直观、适合答辩

## 3. 系统总体设计

### 3.1 系统架构

系统采用 Rust workspace 进行组织，包含以下核心模块：

- `common`：定义共享数据结构
- `parser`：负责日志解析
- `aggregator`：负责指标聚合
- `server`：负责输入接入、HTTP 服务和告警
- `generator`：负责生成测试和演示日志
- `web`：负责网页端展示

系统整体数据流如下：

1. 输入源读取原始日志
2. 日志进入有界队列
3. 解析 worker 并发解析日志
4. 聚合模块串行更新指标
5. API 输出指标
6. dashboard 轮询展示数据

### 3.2 模块划分原则

模块划分遵循以下原则：

- 高内聚、低耦合
- 数据结构统一
- 便于单元测试
- 便于多人分工开发

### 3.3 技术选型

项目使用如下主要技术：

- 语言：Rust
- 异步运行时：Tokio
- Web 框架：Axum
- 序列化：Serde
- 前端：原生 HTML/CSS/JavaScript
- 错误处理：`anyhow`、`thiserror`

选择这些技术的原因是：

- Rust 具备高性能与内存安全优势
- Tokio 和 Axum 适合实现异步网络服务
- 原生前端方案轻量且演示稳定
- Workspace 结构适合课程项目分模块开发

## 4. 详细设计与实现

### 4.1 数据模型设计

系统中的核心结构体为 `LogEvent`，字段包括：

- `timestamp`
- `path`
- `status`
- `latency_ms`
- `method`
- `service`

围绕该结构，系统还设计了：

- `MetricsResponse`
- `WindowMetrics`
- `PipelineStats`
- `Alert`

这使得后端接口、聚合逻辑和前端展示能够围绕统一模型展开。

### 4.2 日志解析器设计

项目支持两种解析方式：

#### 4.2.1 JSON 解析器

JSON 解析器用于处理一行一个 JSON 对象的日志格式，适合结构化程度较高的日志。

#### 4.2.2 access 解析器

access 解析器支持简化版空格分隔日志，例如：

```text
2026-04-20T12:00:00Z GET /access/orders 200 18 gateway
```

通过引入 `ParserKind` 与统一解析接口，系统具备了插件式解析器的基础结构。

### 4.3 并发 pipeline 设计

为了让系统更接近真实流式处理平台，本项目没有采用简单的串行处理，而是构建了并发 pipeline：

- 输入线程负责读取日志
- 原始日志进入 bounded channel
- 多个 worker 并发执行解析任务
- 单独的聚合任务顺序更新指标状态

这种设计具有以下优点：

- 可利用多核提高解析吞吐
- 聚合阶段保持顺序一致性
- 队列长度可用于观察系统负载情况

### 4.4 聚合指标设计

系统维护的主要全局指标包括：

- `total`
- `valid`
- `invalid`
- `errors`
- `error_rate`
- `latency_avg_ms`
- `latency_max_ms`
- `latency_p50_ms`
- `latency_p95_ms`
- `latency_p99_ms`
- `status_counts`
- `top_paths`
- `uptime_secs`

此外，系统还维护：

- `pipeline` 指标
- `10s / 1m / 5m` 窗口指标

### 4.5 时间窗口设计

为了更真实地反映系统近期状态，项目增加了三个滚动观察窗口：

- `10s`
- `1m`
- `5m`

窗口指标包括：

- 请求总数
- 错误数
- 错误率
- QPS
- 平均延迟
- 最大延迟
- p50 / p95 / p99

相比全局累计值，窗口指标更适合展示实时波动情况。

### 4.6 分位数设计

平均延迟虽然能够反映整体趋势，但对长尾请求不敏感。因此系统加入了 `p50 / p95 / p99` 分位数指标。

项目使用有界 histogram bucket 统计分位数，原因如下：

- 内存占用可控
- 更新速度快
- 实现难度适中
- 足以满足课程项目需求

### 4.7 背压与丢弃策略设计

当日志输入速率过高时，系统需要对积压进行控制。为此项目实现了以下策略：

- `block`
- `drop_newest`
- `drop_oldest`
- `sample_1_in_n`

同时记录：

- `dropped_lines`
- `drop_rate`

这部分体现了系统在负载压力下的工程处理思路。

### 4.8 告警模块设计

项目实现了两类基础告警：

- 错误率持续超过阈值
- p95 延迟持续超过阈值

服务通过 `/alerts` 接口返回当前活动告警，包含：

- 告警名称
- 当前值
- 阈值
- 起始时间
- 持续时长

### 4.9 TCP 输入与日志生成器

为了增强演示效果，系统支持 TCP 输入，并实现了独立 `generator` crate 用于模拟日志流。

生成器支持以下模式：

- `normal`
- `spike_error`
- `spike_latency`
- `wave_qps`

利用生成器可以方便地制造错误率上升、延迟升高和流量波动等场景。

### 4.10 前端 dashboard 设计

dashboard 使用原生 HTML/CSS/JavaScript 编写，通过轮询 `/metrics` 和 `/alerts` 获取数据。

前端主要展示内容包括：

- 全局指标卡片
- 多窗口指标卡片
- 状态码统计
- 热点路径统计
- pipeline 统计
- 告警面板

这种实现方式简单、稳定，适合课程答辩演示。

## 5. 系统测试与结果分析

### 5.1 单元测试

项目已针对核心模块编写单元测试：

- `parser`
  - JSON 解析测试
  - access 解析测试
  - 非法输入测试
- `aggregator`
  - 全局聚合测试
  - Top-N 测试
  - 窗口统计测试
- `generator`
  - 不同模式输出测试
  - stdout/TCP 推送测试

### 5.2 自动化质量检查

当前项目通过以下命令验证：

```bash
cargo fmt --all
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace
```

### 5.3 CI 验证

项目配置了 GitHub Actions，对以下平台进行自动化检查：

- macOS
- Windows

检查内容包括：

- `cargo fmt --all --check`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo test --workspace`

### 5.4 人工运行验证

除自动测试外，项目还完成了以下人工验证：

- JSON 文件输入验证
- access 文件输入验证
- TCP 输入验证
- generator 推流验证
- `/health`、`/metrics`、`/alerts` 接口验证
- dashboard 页面展示验证

### 5.5 测试结果分析

从测试结果来看，系统已经能够稳定完成：

- 多输入源接入
- 多解析器解析
- 多窗口统计
- 分位数计算
- 告警触发
- 页面展示

项目达到了课程大作业“可运行、可测试、可展示”的核心要求。

## 6. 项目亮点与创新点

### 6.1 并发 pipeline

相比简单串行统计，本项目实现了更接近真实流处理系统的并发 pipeline。

### 6.2 多时间窗口统计

系统支持 `10s / 1m / 5m` 多窗口指标，更适合展示短期波动。

### 6.3 延迟分位数分析

通过 `p50 / p95 / p99`，系统能够展示长尾请求情况。

### 6.4 背压与采样策略

系统在高负载场景下具备基本控制手段，具有工程意义。

### 6.5 告警与日志生成器闭环

项目具备“推流 -> 指标变化 -> 告警触发 -> 页面展示”的完整演示闭环。

## 7. 存在问题与改进方向

### 7.1 当前存在的问题

- dashboard 仍采用 polling，可进一步升级为 WebSocket/SSE
- 分位数统计为近似桶算法，精度仍可继续提升
- 当前告警规则较少
- 尚未实现历史存储与查询
- 尚未做更细粒度的服务级分析

### 7.2 后续改进方向

- 增加更多告警规则
- 增加服务维度和方法维度统计
- 扩展更多日志格式解析器
- 增加持久化存储与历史查询
- 提升前端实时交互体验

## 8. 总结

本项目实现了一个从日志输入、解析、聚合到展示和告警的完整实时日志分析系统。系统不仅满足课程大作业的基本要求，还进一步加入了并发 pipeline、时间窗口、分位数分析、背压策略、告警与日志生成器等增强能力，使项目具备较强的工程感和展示效果。

总体而言，`logscope` 是一个结构完整、功能闭环、适合课程答辩的实时日志分析平台。该项目既体现了 Rust 在系统开发中的优势，也展示了团队在软件工程组织、模块划分和功能扩展上的设计思路。

## 附录 A：运行与演示命令

### A.1 基础运行

```bash
cargo run -p server -- --addr 127.0.0.1:3000 --file sample.log --format json --workers 4
```

### A.2 access 日志运行

```bash
cargo run -p server -- --addr 127.0.0.1:3000 --file sample_access.log --format access --workers 4
```

### A.3 TCP 推流演示

```bash
cargo run -p server -- --addr 127.0.0.1:3000 --tcp 127.0.0.1:9001 --workers 4
cargo run -p generator -- --mode spike_error --count 32 --push 127.0.0.1:9001
```

## 附录 B：建议答辩展示顺序

1. 介绍项目背景与目标
2. 展示系统架构图与模块划分
3. 启动 server 和 dashboard
4. 展示 `/metrics`
5. 使用 generator 制造异常流量
6. 展示窗口指标和告警
7. 总结项目亮点与扩展方向
